Dynamic representation of the skills matrix using http://recharts.org/en-US and library for React.

Call `ǹpm start` to view the chart.

Adjust file `data.json` to change positions of the dots on the chart.